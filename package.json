{
  "name": "proxyharvest",
  "version": "26.0.0-github-main",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check proxyharvest.js && node --check patches/ui-v26.js && node --check scripts/build.mjs && node --check api/ai/health.js && node --check api/ai/advise.js && node --check worker/proxyharvest-gateway.js",
    "build": "npm run check && node scripts/build.mjs"
  }
}
