{
  "name": "proxyharvest",
  "version": "25.1.1-main-node-only-redeploy",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node --check proxyharvest.js && node --check api/ai/health.js && node --check api/ai/advise.js && node --check worker/proxyharvest-gateway.js && rm -rf public && mkdir -p public && cp index.html public/index.html && cp proxyharvest.html public/proxyharvest.html && cp proxyharvest.js public/proxyharvest.js"
  }
}
